#!/bin/sh

# 챕터 6: 배포 전 검증 자동화 기반
# Pre-commit hook: 타입 체크 실행

echo "🔍 Running pre-commit checks..."

# TypeScript 타입 체크만 실행 (린트는 CI에서)
npm run typecheck
if [ $? -ne 0 ]; then
  echo "❌ TypeScript check failed"
  exit 1
fi

echo "✅ Pre-commit checks passed"
